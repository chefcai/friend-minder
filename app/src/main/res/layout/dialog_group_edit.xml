<?xml version="1.0" encoding="utf-8"?>
<!--
  Body of the New/Edit group sheet. FRM-174 (DL-2): on FmBottomSheet, so the
  title and the single primary ("Create group" / "Save") come from the base.
  Swatches are a 2 x 4 grid built in GroupEditDialogFragment (48dp swatches,
  24dp apart, 16dp between rows) instead of one clipped horizontal row.
  The grid starts on the gutter, so a selected ring in the first column is
  never clipped; the swatch itself sits 5dp (ring + gap) inside it.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <com.google.android.material.textfield.TextInputLayout
        style="@style/Widget.FM.TextInput"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/hint_group_name">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/nameInput"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:minHeight="48dp"
            android:inputType="textCapWords" />
    </com.google.android.material.textfield.TextInputLayout>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/fm_space_6"
        android:text="@string/label_group_color"
        android:textAppearance="@style/TextAppearance.FM.SectionLabel" />

    <LinearLayout
        android:id="@+id/swatchGrid"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/fm_space_2"
        android:orientation="vertical" />
</LinearLayout>
