<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="@dimen/fm_space_4">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="@dimen/fm_space_4">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/title_missing_contacts"
            android:textAppearance="?attr/textAppearanceHeadlineSmall" />

        <ImageButton
            android:id="@+id/closeButton"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:src="@drawable/ic_close"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/action_cancel" />
    </LinearLayout>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/fm_space_2"
        android:text="@string/label_missing_contacts_explainer"
        android:textAppearance="?attr/textAppearanceBodyMedium"
        android:textColor="?attr/colorOnSurfaceVariant" />

    <!--
      Same NestedScrollView-with-fixed-height shape as
      dialog_add_contact_to_group.xml (GH #72/#76): neither ScrollView nor
      NestedScrollView honors maxHeight, and only NestedScrollView
      negotiates scroll ownership with BottomSheetBehavior. This list is
      expected to stay short in practice (it's bounded by how many friends
      you've added), but there's no hard cap on it, so it gets the same
      fixed-height treatment rather than assuming it'll never need to
      scroll. Unlike that dialog's 40+-candidate case, a short list here
      just leaves empty space below it, which is the accepted trade-off.
    -->
    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="240dp">

        <LinearLayout
            android:id="@+id/missingContactContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical" />
    </androidx.core.widget.NestedScrollView>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="end"
        android:layout_marginTop="@dimen/fm_space_4">

        <Button
            android:id="@+id/closeDialogButton"
            style="@style/Widget.Material3.Button.TextButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/action_cancel" />
    </LinearLayout>
</LinearLayout>
